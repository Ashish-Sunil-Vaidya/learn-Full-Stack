<div class="w-full h-full flex flex-col justify-center items-center">
    <div class="w-full py-10 bg-blue-100 flex flex-col items-center">
        <div class="font-bold">
            Welcome to
        </div>
        <div class="flex items-center gap-2">
            <img src="/logo.png" class="w-[100px]" alt="logo">
            <h1 class="font-bold text-3xl">Weblary</h1>
        </div>
        <div class="font-bold opacity-40">
            A online bookstore demo project using ejs
        </div>
    </div>
    <h1 class="text-center text-xl font-bold text-blue-500 py-5 border-b-2 mb-5">Recently Added</h1>
    <div class="w-[100svw] flex gap-3 flex-wrap justify-center items-center">

        <% books.forEach(book=> { %>
            <img width="150px" height="150px" src="<%= book.coverImagePath %>">
            <% }) %>

    </div>
</div>